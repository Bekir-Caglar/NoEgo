<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoEgo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NoEgo.Starting" parent="Base.Theme.NoEgo" >

        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowSplashScreenBackground">@color/textColor</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_noego_logo_round</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.NoEgo</item>
    </style>
</resources>